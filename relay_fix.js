const fs = require("fs");
let c = fs.readFileSync("apps/codex-plus-manager/src/App.tsx","utf8");

// ??? -> ????
c = c.replace(/{ id: "relay", label: "?????",/g, '{ id: "relay", label: "????",');
c = c.replace(/???/g, "??");
// ?"???"??? ??????????

// ??LDCodex -> ????
c = c.replace("?? LDCodex", "????");
c = c.replace("??Codex++", "????");

// ?????? -> ???????
c = c.replace("??????", "???????");

// ?????
c = c.replace(
  '<span className="sidebar-footer-en">LET IMAGINATION BECOME REALITY</span>',
  '<span className="sidebar-footer-en">???????</span>'
);

// Debug/Helper -> ??/??
c = c.replace(
  '<Metric label="????"',
  '<Metric label="????"'
);
c = c.replace(
  '<Metric label="????"',
  '<Metric label="????"'
);

// proxy-info??
c = c.replace(
  'http://127.0.0.1:36001/proxy-info.html',
  'http://127.0.0.1:36001/admin/proxy-info.html'
);

fs.writeFileSync("apps/codex-plus-manager/src/App.tsx", c, "utf8");
console.log("Done");
