{
  "identifier": "default",
  "description": "Default permissions for the LDCodex Manager main window. Grants the dialog plugin's default permission set so the file/folder picker can be opened from settings (fixes #345).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-maximize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-set-theme",
    "core:window:allow-set-size",
    "core:window:allow-set-focus",
    "dialog:default"
  ]
}