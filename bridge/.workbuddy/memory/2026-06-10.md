=== 2026-06-10 ===

- **Fixed: reasoning_content (thinking) dropped in Responses API streaming**
  - Upstream sensenova sends reasoning in delta.reasoning_content field
  - Previously ignored — reasoning was completely dropped from responses
  - Added separate reasoning output item (type: reasoning) in Responses API SSE stream
  - Reasoning appears as output_index=0, message as output_index=1
  - response.completed now includes both reasoning and message items
  - Also fixed non-streaming chatToResponses() to handle msg.reasoning_content
  - File: lib/protocol/openai-responses.mjs
- **Added file upload/download endpoints** (参考旧 proxy.mjs)
  - GET /api/files/download?path= — 下载文件（如 PPT、图片等）
  - POST /api/files/upload?path= — 上传文件（如 Word 文档）
  - File: lib/server.mjs
- **Fixed tool filter** — 不再过滤 CODEX 的 file_search、code_interpreter 等非 function 类型工具
  - 让上游提供者自行决定是否支持这些工具
  - File: lib/protocol/openai-responses.mjs
- **Fixed SSE event format** — 补齐 response.output_text.done 的 text 字段和 response.content_part.done 事件
  - 帮助 CODEX 正确解析 markdown 格式和代码块
- **Fixed Cherry Studio AI_JSONParseError** — 流式 Chat Completions 改为纯透传
  - 优化引入的"逐行解析+JSON修改"模式导致 SSE 格式异常，Cherry Studio 无法正确解析
  - 根因：缺少 \r\n 归一化、SSE 事件边界重建不精确、JSON 正则替换可能破坏数据
  - 恢复为纯透传（与旧 proxy.mjs.bak 一致）：上游 chunk 直接写回客户端，保留原始 SSE 格式
  - 使用 backpressure-aware 写入（等待 drain 事件），避免内存压力
  - 非流式路径保持不变（仍去除 reasoning 字段、替换 model 名）
  - File: lib/protocol/openai-chat.mjs
  - 添加了 response.content_part.done 事件（原来缺失）
  - response.output_text.done 现在包含 text 全文

