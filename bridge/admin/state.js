/* ══════════════════════════════════════════════════════════════ * LUODA中转路由 · 共享状态管理 * 所有页面通过 localStorage 同步状态 * ══════════════════════════════════════════════════════════════ */window.LUODA_STATE = {  // ─── 当前模型管理 ───────────────────────────────────────  // curModels[0] = codexAPI 当前模型, curModels[1] = hermesAPI 当前模型  STORAGE_KEY: 'luoda-cur-models',  LAST_CLICK_KEY: 'luoda-last-model-click',  getCurModels: function() {    try {      var data = localStorage.getItem(this.STORAGE_KEY);      if (data) return JSON.parse(data);    } catch(e) {}    return ['', '']; // 默认空 slug，页面加载时从 API（SQLite）同步  },  saveCurModels: function(arr) {    localStorage.setItem(this.STORAGE_KEY, JSON.stringify(arr));    // 触发存储事件，同源其他标签页可感知    this._notify();  },  // 同时设置 codex 和 hermes 当前模型  setCodexModel: function(id) {    var cur = this.getCurModels();    cur[0] = id;    this.saveCurModels(cur);    return cur;  },  setHermesModel: function(id) {    var cur = this.getCurModels();    cur[1] = id;    this.saveCurModels(cur);    return cur;  },  // 获取当前模型对象  getCodexModel: function() {    var cur = this.getCurModels();    return cur[0] || null;  },  getHermesModel: function() {    var cur = this.getCurModels();    return cur[1] || null;  },  // ─── 10秒轮替点击管理 ───────────────────────────────────  // 第一次点击 → codexAPI, 10秒内再点击 → hermesAPI  // 10秒后重置 → 下一次点击又是 codexAPI  getLastClick: function() {    try {      var data = localStorage.getItem(this.LAST_CLICK_KEY);      if (data) return JSON.parse(data);    } catch(e) {}    return { time: 0, target: 'codex' };  },  // 记录点击并返回应该设置哪个 API  recordClick: function() {    var last = this.getLastClick();    var now = Date.now();    var TEN_SEC = 10000;    if (now - last.time > TEN_SEC) {      // 超时或首次点击 → 设置 codexAPI      var result = { target: 'codex', time: now };      localStorage.setItem(this.LAST_CLICK_KEY, JSON.stringify(result));      return 'codex';    } else {      // 10秒内的第二次点击 → 设置 hermesAPI（然后重置计时）      var result = { target: 'hermes', time: now };      localStorage.setItem(this.LAST_CLICK_KEY, JSON.stringify(result));      return 'hermes';    }  },  // ─── 通知其他页面 ───────────────────────────────────────  _notify: function() {    // 使用 sessionStorage 做跨标签页事件通知    sessionStorage.setItem('luoda-state-update', Date.now());  },  // ─── 主题 ───────────────────────────────────────────────  getTheme: function() {    return localStorage.getItem('luoda-theme') || 'light';  },  setTheme: function(t) {    localStorage.setItem('luoda-theme', t);  },  // ─── 强制切换间隔 ───────────────────────────────────────  getSwitchInterval: function() {    return parseInt(localStorage.getItem('luoda-switch-interval') || '30', 10);  },  setSwitchInterval: function(val) {    localStorage.setItem('luoda-switch-interval', String(val));  },  // ─── 模型快照同步（全页面通用） ──────────────────────────  syncModelsSnapshot: function() {    // 尝试从服务端获取模型列表（含健康状态），只用于 badge 计数，不覆盖快照    fetch('/api/status/models').then(function(r){ return r.json(); }).then(function(list){      if (list && list.length) {        // 只更新 badge 计数，不覆盖 luoda-models-snapshot（保留 models.html 维护的 ID 和顺序）        var badge = document.getElementById('sidebarTotalBadge');        if (badge) badge.textContent = String(list.length);      }    }).catch(function(){      // 离线：从本地缓存读取      try {        var snap = localStorage.getItem('luoda-models-snapshot');        if (snap) {          var arr = JSON.parse(snap);          var badge = document.getElementById('sidebarTotalBadge');          if (badge) badge.textContent = String(arr.length);        }      } catch(e) {}    });  }};