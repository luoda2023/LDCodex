/* ══════════════════════════════════════════════════════════════ * LUODA中转路由 · 统一认证模块 * 双层认证： *   1. 服务端 HttpOnly Cookie（主 — admin server 自动校验） *   2. 客户端 SessionStorage Token（备 — 前端校验 + 超时控制） * ══════════════════════════════════════════════════════════════ */window.LUODA_AUTH = {  TOKEN_KEY: 'luoda-token',  TIMEOUT_KEY: 'luoda-token-issued',  TIMEOUT_MS: 8 * 60 * 60 * 1000, // 8 hours  // 获取当前 Token  getToken: function() {    return sessionStorage.getItem(this.TOKEN_KEY);  },  // 验证 Token 是否有效（客户端 + 服务端双重校验）  isValid: function() {    var token = this.getToken();    if (!token) return false;    // 1. 检查本地超时（快速校验）    var issued = parseInt(sessionStorage.getItem(this.TIMEOUT_KEY) || '0', 10);    if (Date.now() - issued > this.TIMEOUT_MS) {      this.logout();      return false;    }    // 2. 服务端校验（唯一认证方式 — 服务不可用则拒绝访问）    try {      var xhr = new XMLHttpRequest();      xhr.open('GET', '/api/auth/check', false);      xhr.send();      if (xhr.status === 200) {        sessionStorage.setItem(this.TIMEOUT_KEY, String(Date.now()));        return true;      }    } catch(e) {}    // 服务端不可用或返回非 200 → 拒绝访问    this.logout();    return false;  },  // 异步服务端校验（用于页面加载后的额外检查）  serverCheck: function(callback) {    fetch('/api/auth/check')      .then(function(r) {        if (r.ok) {          sessionStorage.setItem(this.TIMEOUT_KEY, String(Date.now()));          if (callback) callback(true);        } else {          this.logout();          if (callback) callback(false);        }      }.bind(this))      .catch(function() {        // 网络不可用时跳过服务端校验        if (callback) callback(true);      });  },  // 登录时调用  login: function(token) {    sessionStorage.setItem(this.TOKEN_KEY, token);    sessionStorage.setItem(this.TIMEOUT_KEY, String(Date.now()));  },  // 登出（双向清除）  logout: function() {    sessionStorage.removeItem(this.TOKEN_KEY);    sessionStorage.removeItem(this.TIMEOUT_KEY);    // 通知服务端清除 HttpOnly Cookie    try {      var xhr = new XMLHttpRequest();      xhr.open('POST', '/api/auth/logout', false);      xhr.send();    } catch(e) {}    // 跳转到登录页    if (window.location.pathname.indexOf('login.html') === -1 &&        window.location.pathname.indexOf('login') === -1) {      window.location.href = 'login.html';    }  },  // 保护页面  protect: function() {    if (!this.isValid()) {      this.logout();      return false;    }    return true;  }};// ─── 自动保护所有非登录页面 ───────────────────────────────────(function(){  // 跳过登录页  if (window.location.pathname.indexOf('login') !== -1) return;  // 页面加载时验证（客户端 + 服务端）  window.addEventListener('DOMContentLoaded', function() {    LUODA_AUTH.protect();    // 额外异步服务端校验    setTimeout(function() {      LUODA_AUTH.serverCheck();    }, 100);  });})();