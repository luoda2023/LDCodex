# 磁盘清理方案（**只读扫描结果，尚未删除任何文件**）

> 依据你的标准：删「中间产物 / 缓存垃圾 / 老代码与参考代码」，
> 留「最新完整源码 + 最新构建的程序」。
> 🔴 另加一条你特别强调的红线：**`obj/` 与 NuGet 包不能碰**，删了机器彻底失去编译能力。

**当前磁盘**：C: 5.4G 可用（91%）｜D: 31G 可用（57%）

---

## 一、🔴 红线 —— 一律不删

| 路径 | 为什么 |
|---|---|
| 任何 `obj/` 目录 | `obj/project.assets.json` 是 NuGet 还原产物，删了 VS 编不动，恢复要重新联网还原 |
| NuGet 包缓存（`~/.nuget/packages` 等） | 同上，装回代价极大 |
| **`D:\LUODA\_LDCodex-keys\ldcodex-updater.key`（+ .pub）** | ⚠️ **自动升级的签名私钥，不可再生**。原在 `_tmp/updater-keys/`，2026-09-15 清 `_tmp` 前已挪到仓库外的 `D:\LUODA\_LDCodex-keys\`。**别再放回 `_tmp/`** —— 那是 `.gitignore` 的临时目录，谁清一次谁就把发版签名能力搞没了（丢了要重新生成密钥对并改 `tauri.conf.json` 的 `pubkey`） |
| `node_modules/`（147M） | npm 的包缓存，性质同 NuGet |
| `target/release/`（5.6G） | **最新构建产物 + 本次安装包**，属「要留」 |
| `.git/` | 源码历史 |
| `crates/`、`apps/codex-plus-manager/src/` | 最新完整源码 |

本机 .NET 工程（其 `obj/` 一并保护）：`D:\LUODA\LDv2rayN\v2rayN.sln`、`D:\LUODA\nttest\nttest.csproj`

---

## 二、建议删（中间产物 / 缓存垃圾 / 老构建）

| # | 路径 | 大小 | 性质 | 代价 / 注意 |
|---|---|---|---|---|
| 1 | `target/debug/` | **7.9G** | debug 中间产物 | 本次出包走的是 release。删了之后下次 `cargo test` 要**冷编约 12 分钟** |
| 2 | `_tmp/` | 219M | 我的临时目录（已 .gitignore） | ⚠️ **删之前先把 `updater-keys/` 挪到安全位置** |
| 3 | `target/release/bundle/nsis/LDCodex_88.8.5_x64-setup.exe` | 51M | **上一版**安装包 | 符合「只留最新构建的程序」 |
| 4 | 根目录散落日志（`_build_*.log`、`_cargo_*.log`、`_ps*.txt`、`_mem.txt`、`_tauri-nobefore.json` 等） | 286K | 缓存垃圾 | 无影响 |
| 5 | `apps/codex-plus-manager/dist/` | 6.7M | 前端中间产物 | ⚠️ `verify-installed-version.mjs` 第 8 项用它里面的 `index-<hash>.js` 当**前端指纹**，删了那条核验会退化成 SKIP（不报错，但失去「装的是不是这轮」的判断力）。跑一次 `npm run build` 即恢复 |

**合计可回收约 8.2G**（主要在 `target/debug`）。

---

## 三、判断标准（以后照这个来）

| 可以删 | 必须留 |
|---|---|
| 生成的中间产物（构建输出、临时目录） | 最终 / 最新版的完整源代码 |
| 缓存垃圾（日志、临时文件、下载残留） | 最新构建出来的程序 |
| 老的代码、参考代码（旧版本副本、备份、实验分支） | 编译依赖缓存：`obj/`、NuGet、`node_modules`、`target/` |

**执行纪律**（我这边默认遵守）：
1. 先只读扫描出清单，**不删任何东西**，等你逐项点头。
2. 一次一小批（≤10 项），删完复验。
3. 能进回收站就不硬删。
4. 扫到 `_tmp/*key*`、`*_keys/`、`*.pem` 之类一律标红线，逐个人工确认 —— 临时目录里常躺着不可再生的密钥。

---

## 四、执行结果（2026-09-15 22:50，用户确认后执行）

| 项 | 结果 |
|---|---|
| 签名私钥 | ✅ 已挪到 `D:\LUODA\_LDCodex-keys\`（仓库外），公钥与 `tauri.conf.json` 逐字节核对一致 |
| 7-Zip（`_tmp/7zfull`，拆 NSIS 包要用） | ✅ 已挪到 `D:\LUODA\_tools\7zfull\` |
| 发版脚本 | ✅ 已入 `tools/create-github-release.mjs`、`tools/verify-github-release.mjs` |
| `target/debug`（7.9G） | ✅ 已删 |
| `_tmp/`（219M） | ✅ 已删 |
| 88.8.5 旧安装包（51M） | ✅ 已删 |
| 根目录散落日志（286K） | ✅ 已删 |
| `apps/codex-plus-manager/dist/`（6.7M） | ⏸ **未删** —— 它里面的 `index-<hash>.js` 是「已装程序是不是这轮」的前端指纹，6.7M 不值得换掉这条守卫 |

**复验**：`git status` 只有 3 个新增文件（无删除/无修改）；`target/release` 与安装包、`.sig` 完好；
`verify-installed-version.mjs` 仍 **8/8 通过**。

**未动**：源码（`crates/`、`src/`、`src-tauri/src/`）、`.git`、`node_modules/`、`target/release/`、
`obj/`、NuGet。

- [x] 删 `target/debug`
- [x] 清 `_tmp`（已先备份私钥）
- [x] 删 88.8.5 旧安装包
- [x] 删根目录散落日志
- [ ] 删 `dist/`（留着，见上）
